pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/EAtech-bit/ML_Project.git'
            }
        }
        // other stages...
    }
}
